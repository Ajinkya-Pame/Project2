// jest.config.js
module.exports = {
  testMatch: [
    "**/tests/**/*.[jt]s?(x)", // Adjust this line to include the tests folder
    "**/?(*.)+(spec|test).[tj]s?(x)"
  ],
  // You can add other configurations if needed
};
